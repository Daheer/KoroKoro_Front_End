export default async function Loading() {
  return (
    <div style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      backgroundColor: 'rgba(0, 0, 0, 0.1)',
      height: '100vh',
      width: '100%',
    }}>
      <img
        src="/loading3d.gif"
        style={{}}
      />
    </div>
  )
}